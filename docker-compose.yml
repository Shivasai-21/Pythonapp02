version: "3.8"
services:
  chatapp:
    build: .
    ports:
      - "3434:5000"
    depends_on:
      - mongo
  mongo:
    image: mongo:5.0
    container_name: chat-mongo
    ports:
      - "27017:27017"
